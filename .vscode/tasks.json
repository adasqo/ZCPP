{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "g++ -g -Wall -o tests/main/main.exe tests/main/main.cpp",
            "args": [
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}